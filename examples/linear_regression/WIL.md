WHAT I LEARNED

1) my first error was to try to predict output starting from output itself, i wasn't doing linear regression, but i'm memorizing data.
- pred = model(y) -> WRONG
- pred = model(x) -> YES!
